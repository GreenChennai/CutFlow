# BUGFIX — BUGREPORT-20260913-纯口播复测 逐条修复记录(v0.8.2)

> 对应报告:`docs/BUGREPORT-20260913-纯口播复测.md`;回归:`tests/test_v9.py`(19 用例,全绿);
> 全量:`tests/` 214 passed。修改副本:repo `skills/cutflow/`(同步到已安装 skill)。

| # | 严重度 | 修复 | 文件 |
|---|---|---|---|
| B1 | 致命·音画 | `step_mix()` 对 `sourceInMs>0` 的音频 clip 增加**输入侧 `-ss` 寻址**(与 step_segment 视频路径同构);`sourceInMs` 同时纳入 mix 步骤缓存键。多段人声不再从源 0s 取。旧绕过法(预抽 `voice_full.wav` 单 clip)仍有效但不再必需 | `rs_render.py` step_mix / render |
| B2 | 致命·渲染 | ① `rs_ir build` 转场改写 **`durMs`**(旧 "ms" 是幽灵字段,静默按 500ms 吞时长);② `rs_ir validate` 显式报错拦截旧字段 `ms` 与非数字 `durMs`;③ `step_concat` 经 `_effective_transitions()` 预计算:**tdur < 1 帧的转场整链弃用**,整体退回无损 concat(xfade 无法与 concat 混用);④ 渲染后新增**音画对齐断言**:成片视频流时长 vs IR 预期(转场吞时已扣)差 >1.5 帧即告警 | `rs_ir.py` / `rs_render.py` step_concat / render |
| B3 | 高 | `rs_subtitle` main:snap 之后**重跑一次 `_enforce_gaps`**(锚点释放余量内收间距),floor/ceil 推回的 30–40ms 重叠被消掉;余量耗尽的残余留给 rs_sync 1 帧容差(对齐精度优先,Hard Rule 20)。`meta.postSnapGaps` 留痕 | `rs_subtitle.py` main |
| B4 | 高 | `rs_cleanup` 重写 06_output 顶层判定 `_out_keep()`:**默认必留** `final_*.mp4`、`subtitles.ass`、`master.srt`、`metadata.*`、`*report*.md`、`cards.json`、`deliverables.md`、`sync_rows.json`、`segments_candidates.json`、`bench_*.png`、中文命名产物;只删 `_`-前缀探针件、`preview_*`/`draft_*`、日志/临时件与非 `sub_*` 目录。符合 ADR-0007 必留清单 | `rs_cleanup.py` |
| B5 | 中 | ① 落盘前新增 `_finalize_events()` 兜底(排序/正时长/无重叠,防倒挂与漂移写入审计件);② `cards.json` 增加 `finalTimes: true` 标注 —— cards.json 与 ass 由**同一份 events** 写出,时间同源同值;③ 回归测试断言 cards.json == ass 逐毫秒一致 | `rs_subtitle.py` |
| B6 | 中 | `rs_bench` 新增 `video_stream_duration()`:**按视频流时长布点**(流 duration → nb_frames/帧率 → 容器时长兜底),音频垫尾不再撑长采样区间;编码后**校验产物存在且非空**,否则 `BENCH_EMPTY` die,拒绝 rc=0 假成功 | `rs_bench.py` |
| B7 | 中 | `events_from_override` 重写:① 定位支持三种 —— 旧 `span`(raw 索引)、新 `text`(去标点后在内容串 S 上**顺序锚定**)、新 `textPrefix+textSuffix`(前缀定位起点、后缀定位终点,中间吞并);② 支持**部分替换**:覆盖不足全部内容字时以 DP 分组为基底,被 override 区间压住的卡被替换,其余沿用 DP(微调一张卡不再重给全部 span);③ 卡尾标点至多带一个;④ 锚定失败显式报错。契约更新:`rules/subtitles.md` §10.3 | `rs_subtitle.py` |
| B8 | 低 | ① `rs_ir build` 检测现存 project.json 的**手注痕迹**(clip.chroma / clip.background / `_meta.manualEdit`),拒绝覆盖(`IR_MANUAL_EDITS`),`--force` 显式确认;② `05_ir/rebuild.py` 生成模板头部写明陷阱与出路(手改 IR → 跑 `06_output/rebuild.py`,S8 只用现有 ass 不碰 IR) | `rs_ir.py` / `rs_run.py` |
| B9 | 低 | `rs_verify.check_subtitles`:字幕缺失 → **skipped(未涉及)**,不再 ✗。`--only S2` 等阶段式运行后的全量 L0 不再误报"字幕合规 ✗"。有 ass 且内容违规仍 FAIL | `rs_verify.py` |
| B10 | 低·验证盲区 | **S9 新增成片音频内容闸**(ADR-0021):`rs_sync --video 成片 --audio-content` —— 成片音轨 ASR 与 wordline 对账(片头句=1 次 / 归一相似度 ≥0.90 / 无重复段),结果缓存 `06_output/audio_check.json`;跑出问题硬失败,工具缺失 skipped 留痕。S9 注册命令固定带上 | `rs_sync.py` / `rs_run.py` |

## 迭代建议(报告 §迭代,本次未实现)

1. DP 断句语义单元惩罚(否定词跨卡/复合词跨卡/"的"字头卡降权)—— 影响**质量**不影响**正确性**,进 BACKLOG;
2. `--terms` 强制喂入与歧义候选显式展示 —— 进 BACKLOG;
3. rs_cut 尾部黑场检测器 —— 进 BACKLOG。

## 既有测试修订

- `tests/test_v5.py::test_verify_l0_catches_broken_subtitles` → 按新契约改名
  `test_verify_l0_missing_subtitles_is_midstate_not_fail`(缺字幕=中间态 skipped,不再硬失败)。
