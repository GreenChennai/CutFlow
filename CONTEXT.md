# CONTEXT — 术语表

> 本文件是 CutFlow 的领域术语表(只放词汇,不放实现)。实现决策见 docs/adr/。

## 素材与输入

- **口播稿视频**:真人出镜讲话的原始视频。若拍摄时带**绿幕/蓝幕**,须先由用户**自行抠像并合成背景**,再把处理完的成片交给 CutFlow(v0.14,ADR-0031)。
- **预处理素材**:已完成抠像与背景合成的口播素材(S0 检测无未处理幕布后才进入剪辑)。
- **纯文案**:只有文字稿、没有画面的输入。配音后走**纯声音视频**路线。
- **分镜表**:含镜头号/画面描述/台词/时长的剧本表格。
- **原声 / 配音声源**:口播视频自带的真人声音,或 TTS 合成的**音色卡**声音。二者互斥或混用时须在 brief 里声明。

## 视频类型(videoType)

- **videoType**:一级路由,决定管线分支。封闭枚举:`talking-head`(纯口播)/ `talking-head+animation`(口播+动画)/ `pure-animation`(纯动画)。预留扩展位:`screen-recording` / `interview` / `drama` / `film-commentary`(暂不实现)。分册见 `skills/cutflow/rules/video-types/`,取代原 `rules/genres/` 六册(ADR-0018)。
- **纯口播**:真人出镜 + 绿幕抠像 + 字幕;动画密度少或零;粗剪必做。
- **口播+动画**:纯口播 + artboard 卡片;额外要求**流畅性**(缓动/时长)与**贴合性**(卡片时间窗落在所解说句子的时间窗内)。
- **纯动画**:无真人画面,场景卡串联;声音来源二选一 —— 音色卡 TTS 或视频中人物原声。
- **场景卡串联**:纯动画成片的画面组织方式——多张 artboard 卡按旁白时间轴先后铺满主轨,每卡覆盖它所解说的一段旁白(ADR-0027)。
- **卡片↔旁白分组**:决定"哪张卡覆盖哪几句旁白"的对齐关系,按锚点词在旁白文本中出现的位置切分。
- **冻结帧补长**:场景卡动画播完而旁白还没说完时,把卡的最后一帧定格续到旁白结束。
- **热词表**:喂给语音识别的专名清单(brief 术语表),用于纠正专有名词的识别错误。
- **孤卡**:字数过少(如 2–3 字)的字幕卡;能并入相邻卡就并入,并不下必须显式留痕。

## 平台与画幅

- **平台预设**:`templates/platforms.json`,每平台定画幅 / 安全区 / 字号 / 每卡字数 / 风格;由 `rs_subtitle --platform` 消费。
- **画幅**:`rs_common.RATIOS` 是"比例 → 宽高"的唯一真相源(`9x16` / `3x4` / `16x9`);其余模块一律查表,禁止字符串比较。
- **画幅常量生成**:比例/平台预设/帧率集合由 `tools/gen_constants.py` 从 `rs_common.RATIOS` 与 `templates/platforms.json` 生成机器可读常量文件,供 Rust 侧(cutforge-schema)编译期引用;生成物与 Python 侧逐键一致(门禁校验零漂移),CutForge 侧不得硬编码任何平台参数。

## 时间线与渲染

- **IR**(Intermediate Representation):毫秒级项目时间线 project.json,成片与剪映草稿的唯一事实源。
- **成片 A**(口播精修):以预处理后的口播素材(已抠像+合成背景)为主体,字幕 + 穿插动画 + 音效的完整成片。
- **成片 B**(纯声音视频):无真人画面,配音(音色卡)+ 动画场景卡串联 + 字幕的成片。
- **后端**:IR 的 N 个消费者,采用**N 后端注册表**(见「CutForge 与同步」)——现有 **FFmpeg 后端**(rs_render,确定性直出,Golden 基准)、**剪映后端**(rs_jy_draft,生成 5.9 可编辑草稿)、**CutForge 后端**(cutforge-render,与编辑器同源的第三后端)。
- **帧量化**:段边界时间吸附到帧网格,消除半帧散差;成片时间轴以帧为最小单位。
- **尾帧扩展**:渲染段时多取与下一段转场等长的源尾帧,供拼接期交叉溶解重叠消费;重叠只落在扩展帧内,成片时间零漂移。
- **交叉溶解提升(joinCrossfadeMs)**:亚帧转场意图(柔化衔接)自动提升为溶解;`0` 显式禁用。

## 品牌

- **Logo 内容包围盒**:Logo 图片 alpha 通道中非透明内容的真实范围;排版以内容尺寸为准,透明 padding 不算。
- **字幕带避让(lifted)**:底部锚点 Logo 自动抬升到字幕带上缘的排版行为,交付说明须标注。

## 字幕

- **字幕卡**:一条屏幕上同时显示的字幕(≤2 行,9:16 每行 ≤16 字)。
- **轻改写**:仅作用于字幕文本的规范化(删口水词、标点规范化、按语义断行),**不改语义、不删信息**;音频不动。
- **字幕规范**:Netflix 简体中文 Timed Text 规范 + 抖音低质判定标准的合集,见 rules/subtitles.md。

## 视觉节奏

- **视觉节拍**(visual beat):画面上任何可感知变化(切换/动画/弹字)的最小间隔,目标 2–3 秒。
- **知识卡 / 数据卡 / 对比卡 / 章节卡**:穿插在口播中的 artboard 动画卡(≤8 秒),分别承载定义、数字、对比、章节标题。
- **虚拟背景**:替代原绿幕的静态背景图(由用户在预处理阶段合成;v0.14 起 CutFlow 不再生成或替换)。

## 剪映

- **剪映后端草稿**:5.9 明文 draft_content.json;6.0+ 已加密,永不写入。
- **花字感**:用本地风格字体 + 描边/底衬/动画模拟的"剪映花字"效果,不依赖剪映云端素材。
- **趣味素材**:音效(SFX)、贴图(PNG)、花字的统称,来源约定见 ADR-0005。

## 声音

- **音色卡**:voices/<名>/card.json,含参考音频、提示文本、权重路径;声音克隆的封装单元。
- **预热**:引擎冷启动后先发一次短合成请求,吃掉首句高延迟。

## CutForge 与同步

- **工程真相源**:工程目录里的文件(project.json / wordline.json / cutlist.json / notes.json)是唯一权威;编辑器(CutForge)不持有独立的权威文档,只做文件的投影与命令通道。
- **操作日志(OpLog)**:一次原子状态变更(Op)的追加式序列;每条 Op 必带 `baseRev`(基于哪一版)与 `actor`(谁改的),是双向合并与"AI 改了什么"审计的唯一依据;撤销/重做也产生新 Op。
- **冲突**:两侧基于同一祖先改了同一字段且值不同(冲突码 CF-001~CF-006);必须显式报错并停止写入,禁止"最后写入者获胜"式静默覆盖。
- **锚点**:标注指向时间轴上某个点或某个元素的稳定引用(kind + ref + tMs);被引用元素必须有稳定 `id`,不得依赖数组下标。
- **标注(Note)**:挂在锚点上的一条人机对话消息("这里要怎么改");用户提出 → AI 执行 → AI 带 `opIds` 回执结案;AI 也可反向挂 `author=agent` 标注提问。
- **孤儿标注(orphan)**:锚点无法重定位时的标注状态;显式保留、编辑器可见,**不得静默丢弃**。
- **无头运行(headless)**:不经 UI、由命令/MCP/脚本驱动的查询、渲染与导出。
- **N 后端注册表**:IR 的消费者集合开放注册(`backendId`:`ffmpeg` / `jianying` / `cutforge` …),工程在 project.json 的 `backends` 字段声明启用了哪些后端;新增后端只登记,不改契约。
